# 模型对比说明

本说明介绍软件自带 `models` 目录下 6 个默认模型的特点，帮助你在转换时选择合适的模型。自行放入 `models` 目录的第三方模型不在本说明范围内。

---

## 六个模型逐个介绍

### realesrgan-x4plus

- **适用内容**：三次元图片（照片、写实风格的图）。
- **优点**：通用性最强，细节还原好，写实图质量上限最高。
- **缺点**：模型体积最大（约 32 MB），速度慢，显存占用高；处理二次元图片时容易把线条涂抹成一团。
- **放大倍率**：仅 4 倍。

### realesr-general-x4v3

- **适用内容**：三次元图片（照片、写实风格的图），定位为 x4plus 的**快速档**。
- **优点**：模型小（约 4.6 MB），纯 CPU 环境实测速度约为 realesrgan-x4plus 的 13 倍（见文末实测数据），日常出图首选。
- **缺点**：按官方说明质量略低于 realesrgan-x4plus；不支持去噪强度调节，重度退化（强噪点、强压缩伪影）图片效果弱于 x4plus。
- **放大倍率**：仅 4 倍。

### realesrgan-x4plus-anime

- **适用内容**：二次元图片（插画、动漫截图、手绘稿）。
- **优点**：线条锐利、色块干净，画质上限最高。
- **缺点**：模型较大（约 8.5 MB），速度慢；处理照片会出现「油画感」。
- **放大倍率**：仅 4 倍。

### realesr-animevideov3（x2 / x3 / x4）

- **适用内容**：二次元视频帧，也可以用于二次元图片。
- **优点**：模型极小（约 1.2 MB），处理速度约为 realesrgan-x4plus-anime 的 1.5～3 倍，显存占用低；提供 x2、x3、x4 三档倍率可选。
- **缺点**：画质上限低，细节生成与修复能力弱；同样只适合二次元内容。
- **放大倍率**：x2、x3、x4 三个版本各对应 2 倍、3 倍、4 倍。

---

## 选择速查表

| 你的图片 | 推荐模型 |
|:---|:---|
| 照片、写实图，追求最高质量 | realesrgan-x4plus |
| 照片、写实图，追求速度（CPU 环境日常用） | realesr-general-x4v3 |
| 二次元图，追求最高画质 | realesrgan-x4plus-anime |
| 二次元图，追求速度或显存有限 | realesr-animevideov3 系列 |

---

## 目标倍率与模型倍率不匹配时，软件是如何处理的？

模型的放大倍率是训练时固定的，引擎**不支持任意倍率参数**，每次调用都严格按模型自身倍率放大。当你选择的目标倍率与模型倍率不一致时，软件的处理方式是**连续调用同一模型多趟**，而不是调参数一次到位：

- **目标倍率恰好是模型倍率的整数次幂**：连跑多趟，刚好命中。例如用 x2 模型放大 8 倍：2→4→8，共 3 趟，无额外处理。
- **目标倍率不是整数次幂**：先放大到**超过**目标的尺寸，最后再用降采样算法（默认 Lanczos，以「高级设定」中选择的为准）缩回目标尺寸。例如用 x4 模型放大 8 倍：4→16，再降采样缩回 8 倍。
- **勾选「预放大」时**：先用 Lanczos 插值做一次预放大，把剩余倍数凑成模型倍率的整数次幂，让 AI 推理趟趟命中、避免末端降采样。例如勾选后用 x4 模型放大 8 倍：先预放大到 2 倍，再 4→8 两趟命中。

注意事项：

- 每趟的输出是下一趟的输入，图片越放越大，**后几趟的耗时和显存占用会显著高于第一趟**。用 x2 模型放大 8 倍，总耗时远大于单趟的 3 倍。
- 倍率不是模型倍率整数次幂时的「过冲再降采样」会多消耗一整趟最大尺寸的推理；在意速度时，优先选择能整除目标倍率的模型，或勾选「预放大」。
- 多趟串联意味着前一趟的瑕疵会被后一趟继续放大，画质上限低的模型（如 animevideov3 系列）叠跑多次后涂抹感会更明显。

---

## 使用建议

- **倍率选择**：同一系列模型的不同倍率版本中，建议选择等于或大于目标放大倍率的版本。例如想放大 3 倍但只有 x2 和 x4 模型时，应选择 x4 的模型，放大后再降采样缩小到目标尺寸。
- **拆分大小（Tile Size）**：显存充足时设大一些，处理速度更快、细节更好；日常使用「自动设定」即可。
- **TTA 模式**：理论上能稍微提升质量，但实际效果几乎看不出来，处理时间却会翻数倍，一般不需要开启。

---

## 常见场景问答

### 我想把手绘稿放大后印刷成海报，该用哪个模型？

首选 `realesrgan-x4plus-anime`（二次元/手绘）或 `realesrgan-x4plus`（写实）。如果一次放大达不到目标分辨率，可以把输出结果作为输入再跑一次，叠跑多次。animevideov3 系列画质上限低，不适用于印刷级输出。

### 我用 anime 模型处理照片，结果像油画一样，怎么办？

换用 `realesrgan-x4plus`。带 anime 的模型只针对二次元内容优化，处理三次元照片必然出现涂抹感。

### 处理速度太慢了，有更快的选择吗？

三次元照片可以换用 `realesr-general-x4v3`，纯 CPU 环境实测比 x4plus 快约 13 倍，代价是质量略降（官方定位为轻量快速模型）。二次元内容可以换用 realesr-animevideov3 系列，速度约为 realesrgan-x4plus-anime 的 1.5～3 倍，代价是画质上限降低。此外也可以通过调大拆分大小（需显存充足）或降低输出尺寸来提速。

---

## 实测速度数据（realesr-general-x4v3 vs realesrgan-x4plus）

测试条件：1024×1024 PNG 放大 4 倍至 4096×4096，单次计时，引擎为内置 realesrgan-ncnn-vulkan（2022-04-24 版），机器为 AMD Ryzen 9 9955HX + 核显（RADV），系统 Vulkan 软渲染为 llvmpipe。

| 运行路径 | realesr-general-x4v3 | realesrgan-x4plus |
|:---|:---|:---|
| llvmpipe（纯 CPU 软渲染） | 约 10.5 秒 | 约 141.5 秒（x4v3 快约 13.5 倍） |
| RADV 核显（Vulkan） | 约 17 秒 | 数据不可采信（见下注） |

注：本机 RADV 核显路径下，x4plus 处理 512×512 及以上尺寸输出纯黑（既有环境缺陷，与 x4v3 新增无关），故其耗时数据不可采信；x4v3 在两条路径下输出均正常。本引擎不支持 `-g -1` 纯 CPU 参数（报 invalid gpu device），CPU 环境请走默认「自动」或 llvmpipe 路径。
