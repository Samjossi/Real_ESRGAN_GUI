# 模型对比说明

本说明介绍软件自带 `models` 目录下 5 个默认模型的特点，帮助你在转换时选择合适的模型。自行放入 `models` 目录的第三方模型不在本说明范围内。

---

## 五个模型逐个介绍

### realesrgan-x4plus

- **适用内容**：三次元图片（照片、写实风格的图）。
- **优点**：通用性最强，细节还原好。
- **缺点**：模型体积最大（约 32 MB），速度慢，显存占用高；处理二次元图片时容易把线条涂抹成一团。
- **放大倍率**：仅 4 倍。

### realesrgan-x4plus-anime

- **适用内容**：二次元图片（插画、动漫截图、手绘稿）。
- **优点**：线条锐利、色块干净，画质上限最高。
- **缺点**：模型较大（约 8.5 MB），速度慢；处理照片会出现「油画感」。
- **放大倍率**：仅 4 倍。

### realesr-animevideov3（x2 / x3 / x4）

- **适用内容**：二次元视频帧，也可以用于二次元图片。
- **优点**：模型极小（约 1.2 MB），处理速度约为 realesrgan-x4plus-anime 的 1.5～3 倍，显存占用低；提供 x2、x3、x4 三档倍率可选。
- **缺点**：画质上限低，细节生成与修复能力弱；同样只适合二次元内容。
- **放大倍率**：x2、x3、x4 三个版本各对应 2 倍、3 倍、4 倍。

---

## 选择速查表

| 你的图片 | 推荐模型 |
|:---|:---|
| 照片、写实图 | realesrgan-x4plus |
| 二次元图，追求最高画质 | realesrgan-x4plus-anime |
| 二次元图，追求速度或显存有限 | realesr-animevideov3 系列 |

---

## 使用建议

- **倍率选择**：同一系列模型的不同倍率版本中，建议选择等于或大于目标放大倍率的版本。例如想放大 3 倍但只有 x2 和 x4 模型时，应选择 x4 的模型，放大后再降采样缩小到目标尺寸。
- **拆分大小（Tile Size）**：显存充足时设大一些，处理速度更快、细节更好；日常使用「自动设定」即可。
- **TTA 模式**：理论上能稍微提升质量，但实际效果几乎看不出来，处理时间却会翻数倍，一般不需要开启。

---

## 常见场景问答

### 我想把手绘稿放大后印刷成海报，该用哪个模型？

首选 `realesrgan-x4plus-anime`（二次元/手绘）或 `realesrgan-x4plus`（写实）。如果一次放大达不到目标分辨率，可以把输出结果作为输入再跑一次，叠跑多次。animevideov3 系列画质上限低，不适用于印刷级输出。

### 我用 anime 模型处理照片，结果像油画一样，怎么办？

换用 `realesrgan-x4plus`。带 anime 的模型只针对二次元内容优化，处理三次元照片必然出现涂抹感。

### 处理速度太慢了，有更快的选择吗？

如果处理的是二次元内容，可以换用 realesr-animevideov3 系列，速度约为 realesrgan-x4plus-anime 的 1.5～3 倍，代价是画质上限降低。三次元照片没有对应的轻量模型，只能通过调大拆分大小（需显存充足）或降低输出尺寸来提速。
