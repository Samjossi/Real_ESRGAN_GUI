# 常用命令

```bash
uv run main.py
```

测试（项目根目录下执行，详见 `tests/README.md`）：

```bash
uv run tests/smoke_test.py          # 端到端冒烟（真实跑引擎，数分钟）
uv run tests/theme_smoke_test.py    # 主题切换冒烟（秒级）
uv run tests/black_image_probe.py --tag post   # 纯黑缺陷视觉验证探针
```
